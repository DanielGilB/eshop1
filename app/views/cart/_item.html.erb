<%= link_to item.disc.title, :action => 'show', :controller => 'catalog', :id => item.disc.id %>

<%= pluralize item.amount, "disc", "discs" %>, <%= sprintf "%0.2f €", item.price * item.amount %>

(<%= link_to '<b>-</b>'.html_safe, :controller => 'cart', :action => 'remove', :id => item.disc %>)
